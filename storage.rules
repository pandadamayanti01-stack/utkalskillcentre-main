rules_version = '2';

// Craft rules based on data in your Firestore database
// allow write: if firestore.get(
//    /databases/(default)/documents/users/$(request.auth.uid)).data.isAdmin;
service firebase.storage {
  match /b/{bucket}/o {
    
    // Allow users to upload and view their own monthly test evidence
    match /monthly_test_evidence/{userId}/{allPaths=**} {
      allow read: if request.auth != null && (
        request.auth.uid == userId ||
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin'
      );
      allow write: if request.auth != null && 
                    request.auth.uid == userId &&
                    request.resource.size < 10 * 1024 * 1024 && // 10MB Max
                    request.resource.contentType.matches('image/.*'); // Images only
    }
    
    // Allow users to upload and view their own rough notes for monthly tests
    match /rough_notes/{userId}/{allPaths=**} {
      allow read: if request.auth != null && (
        request.auth.uid == userId ||
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin'
      );
      allow write: if request.auth != null && 
                    request.auth.uid == userId &&
                    request.resource.size < 10 * 1024 * 1024 && // 10MB Max
                    request.resource.contentType.matches('image/.*'); // Images only
    }

    // Allow users to upload their profile pictures
    match /profile_pictures/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && 
                    request.auth.uid == userId &&
                    request.resource.size < 5 * 1024 * 1024 && // 5MB Max
                    request.resource.contentType.matches('image/.*'); // Images only
    }

    // Allow reading and writing of textbook chapter PDFs (reads are public, writes restricted to authenticated admins)
    match /chapters_pdf/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && (
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
        request.auth.token.email == 'pandadamayanti01@gmail.com' ||
        request.auth.token.phone_number == '+919337956168' ||
        request.auth.token.phone_number == '9337956168'
      );
    }

    // Allow reading and writing of historical textbook chapter PDFs stored in "Chapter Wise Text Book" (reads are public, writes restricted to authenticated admins)
    match /{folder}/{allPaths=**} {
      allow read: if folder == 'Chapter Wise Text Book' || folder == 'Chapter%20Wise%20Text%20Book';
      allow write: if request.auth != null && (folder == 'Chapter Wise Text Book' || folder == 'Chapter%20Wise%20Text%20Book') && (
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
        request.auth.token.email == 'pandadamayanti01@gmail.com' ||
        request.auth.token.phone_number == '+919337956168' ||
        request.auth.token.phone_number == '9337956168'
      );
    }

    // Allow reading and writing of textbook chapter covers (reads are public, writes restricted to authenticated admins)
    match /chapters_covers/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && (
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
        request.auth.token.email == 'pandadamayanti01@gmail.com' ||
        request.auth.token.phone_number == '+919337956168' ||
        request.auth.token.phone_number == '9337956168'
      );
    }

    // Allow reading and writing of community files (segregated by classId)
    match /community_files/{classId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && 
                    request.resource.size < 10 * 1024 * 1024 && // 10MB Max
                    (request.resource.contentType.matches('image/.*') || 
                     request.resource.contentType.matches('application/pdf') || 
                     request.resource.contentType.matches('audio/.*'));
    }

    // Allow reading and writing of revision posters (reads are public, writes restricted to authenticated admins)
    match /revision_posters/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && (
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
        request.auth.token.email == 'pandadamayanti01@gmail.com' ||
        request.auth.token.phone_number == '+919337956168' ||
        request.auth.token.phone_number == '9337956168'
      );
    }

    // Allow reading and writing of textbooks (reads are public, writes restricted to authenticated admins)
    match /textbooks/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && (
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
        request.auth.token.email == 'pandadamayanti01@gmail.com' ||
        request.auth.token.phone_number == '+919337956168' ||
        request.auth.token.phone_number == '9337956168'
      );
    }

    // Allow reading and writing of 3D GLB models (reads are public, writes restricted to authenticated admins)
    match /3D_Models_glb/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && (
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role == 'admin' ||
        request.auth.token.email == 'pandadamayanti01@gmail.com' ||
        request.auth.token.phone_number == '+919337956168' ||
        request.auth.token.phone_number == '9337956168'
      );
    }

    // Default deny for everything else (Strict security default)
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
